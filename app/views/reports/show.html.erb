<p id="notice"><%= notice %></p>

<p>
  <strong>Commentable type:</strong>
  <%= @report.commentable_type %>
</p>

<p>
  <strong>Commentable:</strong>
  <%= @report.commentable_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @report.user_id %>
</p>

<p>
  <strong>Body:</strong>
  <%= @report.body %>
</p>

<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>

<br>
<%= render partial: "comments/comments", locals: {commentable: @report} %>
<%= render partial: "comments/form", locals: {commentable: @report} %>
